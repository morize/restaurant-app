type Query {
  items: [Item]
  item(id: ID!): Item
  itemsByPrice(min: Float!, max: Float!): [Item]
}

type Mutation {
  addNewItem(id: ID!, description: String!, price: Float!, type: String!): Item
}

type Item {
  id: ID!
  description: String!
  price: Float!
  type: String!
}
