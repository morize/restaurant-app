type Query {
  getItem(id: ID!): Item
  getAllItems: [Item]
  getItemsByPrice(min: Float!, max: Float!): [Item]
}

type Mutation {
  createItem(
    name: String!
    description: String
    price: Float!
    type: String!
  ): Item

  updateItem(
    id: ID!
    name: String
    description: String
    price: Float
    type: String
  ): Item

  deleteItem(id: ID!): Item
}

type Item {
  _id: ID!
  name: String!
  description: String
  price: Float!
  type: String!
}
